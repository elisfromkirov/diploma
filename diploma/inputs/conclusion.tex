\section{Заключение}

% Main points
% > task completed
% > further work (rewrite on C++, integrate to engines, animation filtering + (citation))

% Example of usage: Motion Balance Filtering
% Example of usage: Game Engine Plugin

% Algorithm: collision support
% Algorithm: (?)

% Implementation: More fast implementation on C++

% Видимо тут можно похвалить систему не знаю ...
% Отметим, что система обладает возможностью тонкой настройки, при использовании.
% Однако если это не нужно -- то, благодаря формулировке задачи оптимизации система начнет работать без особого тюнинга

% Отметим, что разработанная система обладает возможность тонкой настройки, получаемого движения.